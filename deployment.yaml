apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypto-app-deployment
  labels:
    app: crypto-app
spec:
  replicas: 2  # Number of app instances (replicas)
  selector:
    matchLabels:
      app: crypto-app
  template:
    metadata:
      labels:
        app: crypto-app
    spec:
      containers:
        - name: crypto-app
          image: 654654534276.dkr.ecr.eu-north-1.amazonaws.com/crypto-price:latest  # Updated with your ECR image
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: crypto-app-service
spec:
  type: LoadBalancer  # This makes your app accessible from outside the cluster
  selector:
    app: crypto-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
